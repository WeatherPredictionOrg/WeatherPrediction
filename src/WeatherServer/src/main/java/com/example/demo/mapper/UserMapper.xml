<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <select id="getUserList" parameterType="" resultType="List<User>">
        Select * from member
    </select>
    <select id="insertUser"  resultType="">
        insert into member(username,password) values(#{0},#{1})
    </select>
    <select id="deleteUser" parameterType="String" resultType="">
        insert into member(username,password) values(#{username},#{password})
    </select>
    
</mapper>